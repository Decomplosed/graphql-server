import React from 'react'
import { Card, Icon, Label } from 'semantic-ui-react'

function PostCard({props: {id, body, createdAt, username, likeCount, commentCount, likes}}) {
  return ()
}

export default PostCard